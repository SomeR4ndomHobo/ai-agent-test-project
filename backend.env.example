# Set these in your deployment host's environment.
# Keep your existing OPENAI_API_KEY and TAVILY_API_KEY in .env for local use.
BACKEND_ACCESS_TOKEN=replace-with-a-random-secret-at-least-32-characters
ALLOWED_ORIGINS=https://card-lab-research.workspace-838424.chatgpt.site
PORT=8080
# Set DATA_DIR=/data when deploying with a persistent volume at /data.

